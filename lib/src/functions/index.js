const { account, achievements, friend, message, party, presence, recentPlayers } = require('../http/fetch.js')


module.exports = {
	account: account,
	achievements: achievements,
	friend: friend,
	message: message,
	party: party,
	presence: presence,
	recentPlayers: recentPlayers,
}